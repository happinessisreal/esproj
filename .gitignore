# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/

# --- Jupyter ---
.ipynb_checkpoints/

# --- OS / editor junk ---
*:Zone.Identifier
.DS_Store
Thumbs.db

# --- Generated by ml_model/AQI_Prediction_Model.ipynb ----------------------
# Raw API download + intermediate outputs (always regenerable).
openaq_dhaka_pm25.csv
model_metrics.csv
*.png
# ...but keep the charts extracted from the notebook (deliverables)
!documentation/figures/
!documentation/figures/*.png

# Model bundle + dashboard inputs the app reads at startup (live in their folders).
# Kept on disk so `python backend/app.py` runs immediately, but excluded from VCS.
# To ship sample data so the app works on a fresh clone, force-add them:
#   git add -f dataset/training_data.csv backend/dashboard_data.csv ml_model/aqi_model.joblib \
#              ml_model/metrics.json ml_model/model_card.json \
#              ml_model/predictions_actual_vs_predicted.csv backend/network.json
ml_model/aqi_model.joblib
ml_model/metrics.json
ml_model/model_card.json
ml_model/predictions_actual_vs_predicted.csv
dataset/training_data.csv
backend/dashboard_data.csv
backend/network.json
