# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/

# --- Jupyter ---
.ipynb_checkpoints/

# --- OS / editor junk ---
*:Zone.Identifier
.DS_Store
Thumbs.db

# --- Generated by AQI_Prediction_Model.ipynb -------------------------------
# Raw API download + intermediate outputs (always regenerable).
openaq_dhaka_pm25.csv
model_metrics.csv
*.png

# Model bundle + dashboard inputs the Flask app reads at startup.
# Kept on disk so `python app.py` runs immediately, but excluded from VCS.
# To ship sample data so the app works on a fresh clone, force-add them:
#   git add -f dashboard_data.csv predictions_actual_vs_predicted.csv metrics.json aqi_model.joblib
aqi_model.joblib
dashboard_data.csv
predictions_actual_vs_predicted.csv
metrics.json
model_card.json
network.json
